const size = {
  mobile: '480px',
  laptop: '1440px',
};

const theme = {
  bgColor: '#fafcf9',
  mobile: `(max-width: ${size.mobile})`,
  laptop: `(max-width: ${size.laptop})`,
};

export default theme;
